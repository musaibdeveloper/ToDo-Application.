import React from 'react'

export default function ListItems({ toitems }) {
  return (
      <div className="items-conatiner">
          {toitems.map(() => (
              
          ))}
    </div>
  );
}
